<h1>JE SUIS LA SHOW DU PRODUCT <%= @product.title %></h1>
<h2>Je m'affiche même si je ne suis pas connecté</h2>

<% if user_signed_in? %>
  <%= render "form_booking" %>
<% else %>
  <%= link_to "Réservé", new_user_session_path %>
<% end %>

<% @product.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
<% end %>

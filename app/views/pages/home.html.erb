<div class="container text-center bg-white">
  <h1 class="text-center">Pages#home</h1>
  <h2 class="text-center">All our products</h2>
..
  <div class="row row-cols-1 row-cols-lg-2 g-4">
  <% @products.each do |product| %>
  <%= link_to product_path(product) do %>
    <div class="shadow-sm border rounded p-3 lg-6 text-center position-relative">
      <%= image_tag("bike/biking_hat.jpg", class: 'card-img-top', alt: 'Product Image')%>
     <div class="card-body">
    <h3 class="mt-4"><%= product.title %></h3>
    <%= product.description%>
     <p class="position-absolute bottom-0 end-0 p-3">Price: <%= product.price %>€</p>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

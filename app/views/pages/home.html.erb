<div class="container text-center bg-white mb-5">
  <div class="pt-4">
    <% if user_signed_in? %>
      <div class="text-center">
        <div class="highlight-green-transparent z-2"></div>

        <h1 class="text-center header z-1">Hi <%=current_user.pseudo %></h1>
        <div class="highlight-green"></div>

      </div>
      <p class="text-center">All these <%=Product.count%> gears are waiting for you</p>
    <% else %>
      <h1 class="text-center header">Hi there!</h1>
      <p class="text-center">All these <%=Product.count%> gears are waiting for you</p>
    <% end %>
  </div>

  <div class="row row-cols-1 row-cols-lg-2 g-4">
    <% @products.each do |product| %>
    <%= link_to product_path(product), class:"text-decoration-none" do %>
      <div class="shadow-sm rounded-4 p-3 lg-6 text-center">

        <div class="position-relative">
          <%= image_tag("bike/biking_hat.jpg", class: 'card-img-top rounded-4', alt: 'Product Image')%>
          <div class="position-absolute" class="color-sombre" style="right:10px; bottom:10px; background-color: white; border-radius: 50px; padding: 8px; color:#A2FFA0;font-size: 20px;font-weight: bold; text-shadow: 1px 1px 3px rgba(0,0,0,0.2);">
            <%= product.price %>€
          </div>
        </div>
        <h3 class="mt-4" style="color: black;"><%= product.title %></h3>
        <%# <%= product.description%>
      </div>

    <%# <div class="card-body"> %>
     <%# <div class="position-absolute p-3" style="right:10px; bottom:10px"> %>
          <%# </div> %>
        <%# </div> %>
      <% end %>
    <% end %>
  </div>
</div>
